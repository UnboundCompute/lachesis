export class Store {
  constructor() {
    this.rows = new Map();
  }

  read(key) {
    return this.rows.get(key);
  }

  write(key, value) {
    this.rows.set(key, value);
  }
}

export function openStore() {
  return new Store();
}
