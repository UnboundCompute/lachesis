export class Router {
  constructor() {
    this.routes = new Map();
  }

  get(path, handler) {
    this.routes.set(path, handler);
  }

  dispatch(path, value) {
    return this.routes.get(path)?.(value);
  }
}

export function createRouter() {
  return new Router();
}
